#!/bin/bash
set -x 

mkdir -p $1 $2

BUILD_DIR=$1
CACHE_DIR=$2

echo 'compiling buildpack!'

mkdir -p $BUILD_DIR/.profile.d
#echo './start.sh' > $BUILD_DIR/.profile.d/bash.sh
echo 'bash : ./start.sh' > $BUILD_DIR/Procfile
